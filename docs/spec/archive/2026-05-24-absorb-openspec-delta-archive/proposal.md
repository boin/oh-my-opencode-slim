# Proposal: absorb-openspec-delta-archive

Inspired by https://openspec.dev/ — absorb `spec delta` (changes/<slug>/delta-*.md) and `archive` (archive/YYYY-MM-DD-<slug>/) into this fork.

Implementation: REQ-014/REQ-015 + DES-013/DES-014 + spec_propose/spec_merge/spec_archive tools.

Note: This proposal is recorded retrospectively for archive history. The actual REQ/DES content was inlined to trunk during the same dogfood session, so spec_merge is skipped. spec_archive is invoked next to file this proposal under archive/.
